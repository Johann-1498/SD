services:
  web:
    container_name: flask_app
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ../uploads:/app/uploads
      - ..:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_DEBUG=1
