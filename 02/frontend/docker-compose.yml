services:
  frontend:
    build: .
    environment:
      - SERVER_URL=${SERVER_URL:-http://localhost:5000}
      - API_TOKEN=${API_TOKEN:-admin123}
    ports:
      - "8080:80"
    restart: unless-stopped
